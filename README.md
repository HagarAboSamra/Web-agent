# Web-agent
Traditional web automation systems rely heavily on handcrafted rules or pure DOM parsing. This project instead approaches web interaction as a multimodal reinforcement learning problem

# Installation
pip install miniwob


Alternatively, clone the repository from GitHub:

git clone https://github.com/Farama-Foundation/miniwob-plusplus.git

cd miniwob-plusplus

pip install -e .
